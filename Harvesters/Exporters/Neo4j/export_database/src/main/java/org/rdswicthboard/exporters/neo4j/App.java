package org.rdswicthboard.exporters.neo4j;

import java.io.FileInputStream;
import java.io.InputStream;
import java.util.Properties;


public class App {
/*	private static final String PROPERTIES_FILE = "properties/sync_solr.properties";
	private static final String NEO4J1_FOLDER = "neo4j1"; 
	private static final String NEO4J2_FOLDER = "neo4j2";
	
	private static final ObjectMapper mapper = new ObjectMapper();
	
	public static void main(String[] args) {
		try {
			String propertiesFile = PROPERTIES_FILE;
	        if (args.length > 0 && !StringUtils.isEmpty(args[0])) 
	        	propertiesFile = args[0];
	
	        Properties properties = new Properties();
	        try (InputStream in = new FileInputStream(propertiesFile)) {
	            properties.load(in);
	        }
	        
	        System.out.println("Export Neo4j database");
	                	        
	        String neo4j1 = properties.getProperty("neo4j1", NEO4J1_FOLDER);
	        if (StringUtils.isEmpty(neo4j1))
	            throw new IllegalArgumentException("Neo4j1 Folder can not be empty");
	        System.out.println("Neo4j1 Folder: " + neo4j1);

	        String neo4j1 = properties.getProperty("neo4j1", NEO4J1_FOLDER);
	        if (StringUtils.isEmpty(neo4j1))
	            throw new IllegalArgumentException("Neo4j1 Folder can not be empty");
	        System.out.println("Neo4j1 Folder: " + neo4j1);

		} catch (Exception e) {
			e.printStackTrace();
		}	
	}*/

}
